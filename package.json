{
  "name": "brewerydb-proxy",
  "version": "1.0.0",
  "description": "Firebase function to proxy brewerydb requests for cors support",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.15.4",
    "query-string": "^5.0.0",
    "request": "^2.81.0"
  }
}
